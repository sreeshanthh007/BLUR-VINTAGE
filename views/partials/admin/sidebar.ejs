
<head>
    <style>
          body {
            min-height: 100vh;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
        }

        /* Sidebar Styles */
        .sidebar {
            width: 250px;
            background-color: #e9ecef;
            top: 0;
            left: 0;
            height: 100vh;
            z-index: 1000;
            transition: transform 0.3s ease;
        }

        .sidebar-header {
            padding: 2rem 1.5rem;
            text-align: center;
            background-color: #e9ecef;
            border-bottom: 1px solid #dee2e6;
        }

        .sidebar-header h1 {
            margin: 0;
            font-size: 1.5rem;
            font-weight: bold;
            color: #212529;
        }

        .sidebar-content {
            padding: 1rem 0;
        }

        .nav-item {
            padding: 0.5rem 1.5rem;
        }

        .nav-link {
            color: #495057;
            text-decoration: none;
            padding: 0.75rem 1rem;
            border-radius: 4px;
            transition: all 0.3s ease;
            display: block;
            font-size: 0.95rem;
        }

        .nav-link:hover {
            background-color: #dee2e6;
            color: #212529;
        }

        .nav-link.active {
            background-color: #212529;
            color: #fff;
        }

        .logOutBtn{
            width: 150px;
        }
        .divider {
            height: 1px;
            background-color: #dee2e6;
            margin: 1rem 1.5rem;
        }

        /* Toggle Button */
        .sidebar-toggle {
            display: none;
            position: fixed;
            top: 1rem;
            left: 1rem;
            z-index: 1001;
            padding: 0.5rem;
            background-color: #fff;
            border: 1px solid #dee2e6;
            border-radius: 4px;
        }

        /* Main Content */
        .main-content {
            margin-left: 250px;
            padding: 2rem;
            transition: margin-left 0.3s ease;
        }

        /* Responsive Styles */
        @media (max-width: 991.98px) {
            .sidebar {
                transform: translateX(-100%);
            }

            .sidebar.show {
                transform: translateX(0);
            }

            .sidebar-toggle {
                display: block;
            }

            .main-content {
                margin-left: 0;
            }
        }

        @media (max-width: 575.98px) {
            .sidebar {
                width: 100%;
                max-width: 300px;
            }

            .sidebar-header h1 {
                font-size: 1.25rem;
            }

            .nav-link {
                font-size: 0.9rem;
                padding: 0.5rem 0.75rem;
            }
        }
    </style>
</head>





<button class="sidebar-toggle btn" type="button" aria-label="Toggle Sidebar">
    <i class="bi bi-list"></i>
</button>

<!-- Sidebar -->
<nav class="sidebar">
    <div class="sidebar-header">
        <h1 class="brand-logo">BLUR VINTAGE ★</h1>
    </div>
    <div class="sidebar-content">
        <ul class="nav flex-column">
            <li class="nav-item">
                <a href="/admin/dashboard" class="nav-link active" >
                    <i class="bi bi-speedometer2 me-2"></i>
                    Dashboard
                </a>
            </li>
            <li class="nav-item">
                <a href="/admin/sales-report" class="nav-link active" >
                    <i class="bi bi-speedometer2 me-2"></i>
                    Sales Report
                </a>
            </li>
            <li class="nav-item">
                <a href="/admin/productpage" class="nav-link">
                    <i class="bi bi-box me-2"></i>
                    Products
                </a>
            </li>
            <li class="nav-item">
                <a href="/admin/order-list" class="nav-link">
                    <i class="bi bi-list-check me-2"></i>
                    Order lists
                </a>
            </li>
            <li class="nav-item">
                <a href="/admin/userManage" class="nav-link">
                    <i class="bi bi-people me-2"></i>
                    Users
                </a>
            </li>
            <li class="nav-item">
                <a href="/admin/coupons" class="nav-link">
                    <i class="bi bi-ticket-perforated me-2"></i>
                    Coupons
                </a>
            </li>
            <li class="nav-item">
                <a href="/admin/Category" class="nav-link">
                    <i class="bi bi-grid me-2"></i>
                    Category
                </a>
            </li>
            <li class="nav-item">
                <a href="#" class="nav-link">
                    <i class="bi bi-image me-2"></i>
                    Banners
                </a>
            </li>
            <li class="nav-item">
                <a href="/admin/addOffer" class="nav-link">
                    <i class="bi bi-gift me-2"></i>
                    Offers
                </a>
            </li>
            <div class="divider"></div>
            <li class="nav-item">
                <a href="#" class="nav-link">
                    <i class="bi bi-gear me-2"></i>
                    Settings
                </a>
            </li>
            <li class="nav-item">
                <!-- <a href= class="nav-link"> -->
                    <button style="background-color: black; color: white;" class="logOutBtn" onclick="logout()">  log out         <i class="bi bi-box-arrow-right me-2"></i></button>
                 
                    
                <!-- </a> -->
            </li>
        </ul>
    </div>
</nav>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
    // Sidebar Toggle Functionality
    const toggleButton = document.querySelector('.sidebar-toggle');
    const sidebar = document.querySelector('.sidebar');

    toggleButton.addEventListener('click', () => {
        sidebar.classList.toggle('show');
    });

    // Close sidebar when clicking outside on mobile
    document.addEventListener('click', (e) => {
        if (window.innerWidth < 992) {
            if (!sidebar.contains(e.target) && !toggleButton.contains(e.target)) {
                sidebar.classList.remove('show');
            }
        }
    });


    
        function logout() {
   const response = fetch('/admin/logout', { method: 'GET' })
      .then(response => {
        if (response.ok) {
         
          Swal.fire({
            title: 'Logged Out',
            text: 'You have been successfully logged out!',
            icon: 'success',
            confirmButtonText: 'OK'
          }).then(() => {
            
            window.location.href = "/admin/login";
          });
        } else {
          Swal.fire({
            title: 'Error',
            text: 'There was an issue logging out.',
            icon: 'error',
          });
        }
      })
      .catch(error => {
        console.error('Error:', error);
        Swal.fire({
          title: 'Error',
          text: 'An unexpected error occurred.',
          icon: 'error',
          confirmButtonText: 'Try Again'
        });
      });
  }


</script>